# Sound Effects

Place your TV static sound effect file here as `tv-static.mp3`.

The StaticEffect component will automatically use it when channels are switched.

If the file is not present, the visual static effect will still work, just without audio.

